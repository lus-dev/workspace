<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:bind="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
            name="navigator"
            type="lus.areapass.auth.viewmodels.AuthenticationViewModel" />

    </data>

    <androidx.core.widget.NestedScrollView style="@style/Scroll">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@drawable/form_bgd_auth"
            android:orientation="vertical">

            <include
                layout="@layout/layout_toolbar"
                bind:navigator="@{navigator}" />

            <!-- Focusable uses to prevent setting cursor at first field -->
            <RelativeLayout
                android:id="@+id/content"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:focusable="true"
                android:focusableInTouchMode="true" />

        </LinearLayout>

    </androidx.core.widget.NestedScrollView>

</layout>
